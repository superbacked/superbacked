#! /bin/sh
# Clear-on-death guard (see src/utilities/spawnGuard.ts) — blocks until the
# pipe held by the owning process closes, then clears the pasteboard,
# which outlives the process
read -r _ || true
exec pbcopy < /dev/null
